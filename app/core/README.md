# Pure domain boundary

Phase 1 and later pure SKUForge domain modules live here. Files in this directory may not import React, React Router, Shopify packages, Prisma, or `app/adapters`; ESLint enforces the boundary and the Phase 0 test suite proves the rule rejects a deliberate violation.
